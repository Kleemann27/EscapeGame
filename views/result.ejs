<!DOCTYPE html>
<html lang="et">
<head>
  <meta charset="UTF-8">
  <title>Mäng loodud!</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background-color: #e1f5fe;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 2rem;
      display: flex;
      justify-content: center;
    }

    .container {
      background-color: #fff;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      width: 100%;
      text-align: center;
    }

    h1 {
      color: #0288d1;
      margin-bottom: 1.5rem;
      font-size: 1.8rem;
    }

    p {
      font-size: 1.2rem;
      color: #333;
      margin: 0.7rem 0;
    }

    a {
      color: #0288d1;
      font-weight: bold;
      font-size: 1.2rem;
      word-break: break-word;
      display: inline-block;
      margin-top: 0.5rem;
    }

    .qr {
      margin: 1.5rem auto;
    }

    .qr img {
      max-width: 100%;
      height: auto;
    }

    .info-box {
      margin-top: 2rem;
      background-color: #fff3cd;
      border: 2px dashed #ff9800;
      padding: 1.2rem;
      border-radius: 12px;
      color: #e65100;
      font-weight: bold;
      font-size: 1.1rem;
    }

    .info-box p {
      margin: 0.6rem 0;
    }

    .info-box strong {
      font-size: 1.3rem;
      color: #bf360c;
    }

    @media (max-width: 600px) {
      h1 {
        font-size: 1.6rem;
      }
      p, a {
        font-size: 1.3rem;
      }
      .info-box {
        font-size: 1.2rem;
      }
      .info-box strong {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>✅ Mäng on edukalt loodud!</h1>

    <p>Jaga link õpilastele:</p>
    <a href="<%= url %>" target="_blank"><%= url %></a>

    <div class="qr">
      <p>või skaneeri QR-kood:</p>
      <img src="<%= qr %>" alt="QR-kood" width="180">
    </div>

    <div class="info-box">
      <p>🧠 Selle mängu <strong>vahetundi pääsemise kood</strong> on:</p>
      <p><strong><%= code %></strong></p>
  
    </div>
    <div style="margin-top: 2rem;">
      <p>⏳ Sinu mängu säilitatakse <strong>kuni 24 tundi</strong></p>
    <h1>Kui soovid oma mängu alles hoida ja hiljem kasutada, siis lae see alla</h1>
  <a href="/download/<%= url.split('/').pop() %>">
    <button style="padding: 0.6rem 1.2rem; font-size: 1.1rem; background: #4caf50; color: white; border: none; border-radius: 8px; cursor: pointer;">
      ⬇️ Lae mäng alla
    </button>
  </a>
</div>
  </div>

  


</body>
</html>
